import {chromium,expect} from '@playwright/test';const b=await chromium.launch({headless:true,args:['--no-sandbox','--use-gl=angle','--use-angle=swiftshader','--enable-unsafe-swiftshader']});const p=await b.newPage();await p.goto('http://localhost:3001');await p.getByRole('button',{name:'An island adventure'}).click();await expect(p.getByText('Your island is alive!',{exact:false})).toBeVisible({timeout:25000});await p.getByRole('button',{name:'Back to planet'}).click();await p.getByRole('button',{name:'A daydream garden'}).click();await expect(p.getByText('Your little garden is ready.',{exact:false})).toBeVisible({timeout:25000});await p.getByRole('button',{name:'Back to planet'}).click();await p.getByRole('button',{name:'Your worlds',exact:true}).click();await expect(p.getByRole('button',{name:'A pocketful of sunshine'})).toBeVisible();await expect(p.getByRole('button',{name:'The daydream garden'})).toBeVisible();console.log('Two independent local worlds created and preserved.');await b.close();
