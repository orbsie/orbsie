# Surface formation release

Source `821f396` deployed to `https://orbsie-7h8rxj5l4-grappeggias-projects.vercel.app`, aliased to `https://orbsie.com`. Full suite passed 478 tests with 7 optional skips; production build passed.

Both saved OpenRouter and ChatGPT authored games passed `verify-authored-game-runtime.mjs` replay with the newly built standalone runtime. Their existing world snapshots were retained; no inference was performed. The renderer continuity fixture and prepared-worker checks provide separate evidence for particle handoff, recolor, surface correspondence and transferred attributes.

The production smoke verifies homepage rendering and exact player/worker hashes. Normal-GPU performance, complete Blender distribution, and fresh successful provider/publication acceptance remain separate open requirements.
