# Catalog worker production release

Source `7f76c36` deployed to https://orbsie-ne1x1f0qt-grappeggias-projects.vercel.app and aliased to https://orbsie.com on 2026-09-08.

The read-only production check passed root HTTP 200, anonymous generation journal HTTP 401, exact SHA-256 matches for the player runtime and both geometry workers, and a rendered landing page without page errors. The screenshot was visually reviewed. No generation request was submitted. Functional catalog editor/export/build browser evidence is separate in `../catalog-worker-browser/`.
