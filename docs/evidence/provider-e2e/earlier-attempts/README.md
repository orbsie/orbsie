# Earlier browser attempts

These screenshots document earlier failed harness/configuration attempts, not the current passing reports. The ChatGPT screenshot predates the download-button selector fix. The OpenRouter screenshot shows the local server rejecting a mismatched Origin before reaching the provider; the corrected local server subsequently passed its capped workflow. No API keys or companion capabilities are included.
