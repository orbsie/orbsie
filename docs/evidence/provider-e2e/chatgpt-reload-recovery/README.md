# Real ChatGPT recovery after page reload

This run passed the complete browser workflow using exactly three actual managed ChatGPT generations: `gpt-6-astra`, low reasoning, default processing. The app on port 3017 used the development database and authenticated development account. No fixture generation, OpenRouter, Gateway, or fallback calls were used.

The browser reloaded while the first run was still active at durable sequence/revision 5, with one finished tree. It did not press Stop. After reconnecting the local companion through a new document navigation, the harness opened the same cloud world by ID through the account UI. Recover latest generation settled the active journal to `cancelled` and installed its full finished snapshot with the original prompt. The first five DOM stage observations were retained outside the reloaded page.

Generation count remained one throughout reload, reconnection, and recovery. The explicit continuation was request two and carried the recovered scene and selection context. The existing tree retained its exact entity data and ID while the missing mushroom and game rules were completed. Request three applied only the selected pink material edit.

Reload, ZIP export, standalone score +7/held-input deduplication/win/restart/loss, completed journal recovery, cloud save, and cookie-only fresh-context reopening all passed. The fresh context matched the saved IndexedDB snapshot, made zero generation requests, and reported no page errors. Google Fonts requests were intentionally blocked by the harness. Reports are `wrapper.json` and `chatgpt-local.json`; screenshots include the recovered tree, final edit, and standalone gameplay.

The initial seed evidence is the preserved DOM observer data. No screenshot was taken before interruption, to avoid delaying the reload. This verifies browser document interruption and explicit checkpoint regeneration; it does not claim to resume an underlying provider stream or prove every network-failure condition. Blender packaging, the remaining provider matrix, and dedicated publication remain separate open requirements.

App source: `361f9da`. Its production deployment and smoke check are documented in `docs/cloud-verification.md`. The authenticated live-provider test itself ran against the development app.
