# Integration milestone release

Source `f6e58014983878c6136babfe86c8aaa782bcb4c0` passed the batched local integration checks on 2026-09-08:

- `npm test`: 70 test files passed, 2 skipped; 498 tests passed, 7 skipped.
- `npm run build`: production compilation, TypeScript and static generation passed.
- `npx vercel --prod --yes`: completed successfully and aliased the deployment to `https://orbsie.com`.

Immutable deployment: https://orbsie-3fgwy2jj3-grappeggias-projects.vercel.app

The read-only release verifier passed using this explicit source/deployment and evidence directory. It recorded homepage HTTP 200, anonymous generation-journal HTTP 401, exact player and both geometry-worker hashes, a visible canvas, the exact prompt placeholder, no page errors and no non-GET browser requests. Astra inspected the landing screenshot. A Google Fonts CSS GET is retained in the report.

This deploys the accumulated app changes, including accepted entity-update draw metrics. The candidate installer remains an offline packaging tool; no synthetic Blender archive was published as a user download. The check does not submit a prompt, exercise provider inference, prove per-Orb publication, certify hardware performance, or close the complete Blender runtime gate.
